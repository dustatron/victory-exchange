name: CI

on:
  push:
    branches:
      - master
      - release/*

jobs:
  firebase-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-node@master
        with:
          node-version: '10.x'
      - name: npm install
        working-directory: ./Victory-Exchange-Site
        run: npm install Victory-Exchange-Site

      - name: npm build
        working-directory: ./Victory-Exchange-Site
        run: npm run build:prod Victory-Exchange-Site

      - name: Deploy
        uses: w9jds/firebase-action@master
        working-directory: ./Victory-Exchange-Site
        with:
          args: deploy --only hosting
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
